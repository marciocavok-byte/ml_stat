# O que precisamos:

Criar um ambiente virtual no ambiente
 - No Windows(Terminal do VS CODE): Após selecionar sua pasta de trabalho use no terminal <python -m venv .venv>

    - Ativação: Ao finalizar a criação do ambiente virutal com o comando acima use: <.venv\Scripts\Activate.ps1

Criar um arquivo com a extensão .ipynb

Instalar e importar as bibliotecas necessárias:
!pip install pandas, numpy, scikit-learn
por fim importar essas libs para nosso script, no caso sklearn, dentro de cada bloco vamos importar os módulos específicos para cada aplicação
as demais podemos fazer:
<import pandas as pd>
<import numpy as np>